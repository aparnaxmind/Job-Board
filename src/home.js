
function Home() {

    return (
        <div className="Home">
         
         Already registered ? <a href="login">Login here</a>
         New User ? <a href="registration">Register here</a>
        </div>
      );

}
export default Home;